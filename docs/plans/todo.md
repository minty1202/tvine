# TODO

まだ Issue にしていないが、議論の過程で生まれた対応が必要なことを置く場所。

## 未着手

- `~/.tvine/` 作成時に Claude Code Plugin（`~/.tvine/plugin/`）も一緒に作成する

- `exec` クレートの DI 設計を検討する
  - **背景**: adapter のテスタビリティを検討する中で、book-manager の ConnectionPool パターン（外から依存を注入し、テスト時に差し替える）を参考にした。data（ファイルシステム操作）はベースパスを注入すればテスト用の一時ディレクトリで検証できるが、exec（OS 依存の操作）は「接続先を変える」ことができず、結果自体を差し替えるには trait 化して mock する必要がある
  - **目論見**: exec は現在バイナリの存在確認のみだが、将来的に PTY やプロセス管理など OS 依存の操作全般を担うクレートになる想定。多くの adapter impl が exec に依存することになるため、trait で抽象化して DI 可能にすることで adapter 層を単独でテストできるようにしたい
  - **今していない理由**: 現時点の exec は存在確認だけで、将来 PTY 等が入った時の trait の形がまだ見えていない。今 trait を切っても合わなくて作り直す可能性がある。次に OS 操作が増えるタイミング（PTY 導入時等）で、具体的なユースケースが揃った状態で設計する方が無駄が少ない

- スロットの作成時コマンド・削除時コマンド機能
  - **背景**: スロット管理（#40）の設計時にスコープ外とした
  - **作成時コマンド**: セッション作成時、テンプレートファイル配置後に worktree 上で実行（例: `rails db:create db:migrate db:seed`）
  - **削除時コマンド**: セッション削除時、worktree 削除前に実行（例: `docker compose down`）
  - **実行方式は未決**: バックグラウンド実行か PTY 上で実行するか要検討

## Issue 化済み

- ~~health check で `~/.tvine/` ディレクトリの存在確認をする~~ → #9
- ~~エラー型の設計を決める（thiserror で AppError を shared に定義、AppResult 型エイリアス等）~~ → #8
- ~~exec クレートのリネーム~~ → #27
